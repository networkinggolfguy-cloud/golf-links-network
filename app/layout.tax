import './globals.css'

export const metadata = {
  title: 'Golf Links Network',
  description: 'Where Business Meets the Fairway', }

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
